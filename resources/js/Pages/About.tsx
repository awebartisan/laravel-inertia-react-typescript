import React from "react";

const Home = () => {
    let react: string = "React";
    const typescript: string = "TypeScript";

    return (
        <h1>
            About page
        </h1>
    );
};

export default Home;
